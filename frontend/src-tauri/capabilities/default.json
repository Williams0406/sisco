{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permisos por defecto para SISCO",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "http:default",
    "shell:allow-open"
  ]
}